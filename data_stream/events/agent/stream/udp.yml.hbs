host: '{{listen_address}}:{{listen_port}}'
tags:
  - unifi-events
  - forwarded
fields_under_root: true
fields:
  event.dataset: unifi.events
processors:
  - add_fields:
      target: ''
      fields:
        ecs.version: 8.11.0
